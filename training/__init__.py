"""Model training pipeline (implemented in Phase 3).

Planned modules:

* ``dataset.py``  - load recorded samples from ``datasets/`` into feature arrays
* ``features.py``  - convert landmarks into normalized feature vectors
* ``trainer.py``   - train/validate a classifier and report metrics
                     (accuracy, precision, recall, F1, confusion matrix)
"""
